{
  "name": "backend-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && NODE_ENV=development netlify-lambda serve src",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && NODE_ENV=production ./node_modules/.bin/netlify-lambda build src"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "netlify-cli": "^12.2.7",
    "netlify-lambda": "^2.0.15",
    "serverless-http": "^3.1.0",
    "win-node-env": "^0.6.1"
  }
}
